import React from "react";
import styles from "./ReserveCardDateItem.module.css";

function ReserveCardDateItem({ _onClick }) {
  return (
    <div>
      <button onClick={_onClick}>Test</button>
    </div>
  );
}

export default ReserveCardDateItem;
